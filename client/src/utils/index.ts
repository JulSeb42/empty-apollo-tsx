/*=============================================== Export all utils from ts-utils ===============================================*/

import {
    calculateAverage,
    calculateTotalSum,
    capitalize,
    convertDate,
    convertDateShort,
    convertPrice,
    convertToEmail,
    convertToPascal,
    convertYoutube,
    detectLanguage,
    formatHour,
    generateNumbers,
    getFirstName,
    getLastName,
    getPercentage,
    getRandom,
    getRandomAvatar,
    getRandomDate,
    getRandomNumber,
    getRandomString,
    getRandomTime,
    getTimeNow,
    getToday,
    passwordRegex,
    emailRegex,
    scrollToTop,
    slugify,
    unslugify,
} from "ts-utils-julseb"
import { v4 as uuid } from "uuid"

export {
    calculateAverage,
    calculateTotalSum,
    capitalize,
    convertDate,
    convertDateShort,
    convertPrice,
    convertToEmail,
    convertToPascal,
    convertYoutube,
    detectLanguage,
    formatHour,
    generateNumbers,
    getFirstName,
    getLastName,
    getPercentage,
    getRandom,
    getRandomAvatar,
    getRandomDate,
    getRandomNumber,
    getRandomString,
    getRandomTime,
    getTimeNow,
    getToday,
    passwordRegex,
    emailRegex,
    scrollToTop,
    slugify,
    unslugify,
    uuid,
}
