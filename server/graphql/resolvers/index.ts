/*=============================================== Export all resolvers ===============================================*/

import { Query } from "./queries"

const resolvers = {
    Query,

    // Mutation: {

    // }
}

export default resolvers
